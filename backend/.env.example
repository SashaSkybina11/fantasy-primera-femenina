DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fantasy_futsal?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
PORT=4000
FRONTEND_URL="http://localhost:5173"
# Set this only in Vercel after connecting a Vercel Blob store. It keeps avatar uploads persistent.
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
